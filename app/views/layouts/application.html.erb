<!DOCTYPE html>
<html>
  <head>
    <title>ProgressiveLoadingExample</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", type: "module" %>

    <%= tag.meta name: "turbo-layer", value: "client-detail", src: projects_client_lists_path %>

    <%= yield :head %>
  </head>

  <body>
    <main class="m-2">
      <%= yield %>
    </main>
  </body>
</html>
